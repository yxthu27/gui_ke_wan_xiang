# 贵客万象全功能体验、修复与后续计划

体验基准：模拟首次使用的普通旅行用户，在 360×800、390×844、430×932 三档移动端尺寸完成路线六问、行程生成、手帖/地图切换，并体验随逛点位、个人行径/贵客帖、广场列表与行径详情。

## 本轮发现并已修复

1. **选完答案却不知道如何继续**
   - 原行为：心愿、时间、走法、风物等页面只能点击输入框旁的“发送”图标进入下一问。
   - 修复：每一问新增一致、明确的“继续下一问”主按钮；发送按钮只用于发送文字补充，无输入时禁用。

2. **系统替首次用户预选主观偏好**
   - 原行为：步速、交通、兴趣、边界在用户操作前已有大量默认选择。
   - 修复：首次进入时主观偏好全部为空；继续前进行校验，用户必须作出真实选择。

3. **“阿境已经记住”与实际选择不一致**
   - 原行为：后续页面显示黄果树、非遗等硬编码内容，即使用户没有选择。
   - 修复：所有记忆标签改为读取当前草稿中的真实心愿、时间、步速、兴趣与住宿选择。

4. **抵达/离开地点采用不可预期的循环切换**
   - 原行为：点击“修改”会在三个地点之间循环，用户看不到全部选项。
   - 修复：改为标准地点下拉选择，并提供明确的“抵达地点/离开地点”标签。

5. **边界页有三个互相竞争的完成入口**
   - 原行为：“确认锁定”“看看阿境记住了什么”“发送”都可能完成六问。
   - 修复：收敛为唯一主操作“完成六问，查看确认”；未选择边界时显示就地错误信息。

6. **沉浸流程中一级导航造成误触和割裂**
   - 原行为：收帖、六问、确认、生成和微调期间始终显示一级导航。
   - 修复：这些专注流程隐藏一级导航，结果页恢复导航。

7. **四天方案只显示第一天，“下一天”按钮没有作用**
   - 原行为：手帖和地图始终读取 Day 1；离线方案也只有一天。
   - 修复：手帖与地图新增可横滑日期选择；离线方案按用户选择生成 1–7 天完整内容，日期切换会同步更新主题、时间轴和地图地点。

8. **AI 超时晚于客户端超时**
   - 原行为：客户端 30 秒放弃请求，但服务端最长等待 65 秒，导致用户看到不完整的单日占位方案。
   - 修复：AI 请求控制在 10–25 秒；失败后自动返回完整本地安心方案；客户端网络异常也会立即构建完整多日方案。

9. **广场详情出现 `[object Object]...站`**
   - 原因：把站点对象数组直接插入字符串。
   - 修复：列表、详情、地图摘要统一展示真实站点数量；同时缩短首图，优先让标题和路线信息进入首屏。

10. **随逛地图成为文字标签墙**
    - 修复：默认只显示点位图钉；悬停、键盘聚焦、当前点或选中类别时再显示名称。点位恢复键盘访问和名称标签。

11. **关键触控区域偏小**
    - 修复：六问选项、继续按钮、语音/发送、顶部返回及文字操作统一提升到约 44 px；输入区适配底部安全区。

## 已验证路径

- 新用户进入六问时无主观预选。
- 未选择心愿、交通或边界时无法误进入下一步，错误提示与当前问题同屏。
- 选择的心愿会正确显示在时间、步速、走法、风物和确认页。
- 发送补充按钮在输入为空时禁用。
- 四天本地方案返回 4 个日期，Day 2 主题可切换为“把一整天留给山水大景”。
- 地图支持 Day 1–4 切换，并同步更新日序。
- 广场详情显示“4 站”，不再出现对象字符串。
- 随逛点位名称默认隐藏，点击点位后正常展开预览。
- 360、390、430 px 三档无页面级横向溢出，“继续下一问”始终可达。

## 后续优化计划

### P1：下一轮应完成

- 将路线结果页的“收下这一程”真正写入个人页行程数据，而不只显示提示。
- 将路线显影替换为真实地图路线，当前仍是视觉示意。
- 为个人页保存 PNG、系统分享和发布广场补齐统一成功/失败反馈。
- 模态或全屏详情打开时，让一级导航自动进入弱化或隐藏状态。
- 广场“沿着 TA 走”与路线主规划建立同一份可见数据，而不是仅存 legacy 状态。

### P2：体验深化

- 首页自由描述真正带入开场字幕，而不是固定“瀑布、村寨、街边小店”。
- 将 AI 生成进度与服务端真实阶段关联，避免纯时间估算。
- 加入空收藏、无附近结果、定位拒绝、地图加载失败的完整可恢复状态。
- 为关键用户路径建立自动视觉快照和触控尺寸检查。

本轮优先解决了会让首次用户迷路、误以为系统已替自己决定、或得到不完整结果的问题；保留了现有宣纸与黔山视觉体系。
