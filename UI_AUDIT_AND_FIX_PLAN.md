# 贵客万象 UI 全页审查与修复计划

审查基准：390 × 844 移动端视口。已覆盖“启境”14 个流程屏幕、4 个一级导航页，以及广场行径详情、个人行径详情、贵客帖编辑器、随逛地图筛选状态。截图位于 `audit/screenshots/`。

## 结论摘要

当前视觉语言（宣纸、黔山绿、朱砂、在地插画）辨识度较强，但产品已经进入“视觉精致、操作吃力”的阶段。最优先的问题不是换风格，而是恢复移动端可读性、触控性和信息节奏。

- P0：六问页的绝大多数按钮小于 44 × 44 px；逐页测得心愿 13/13、时间 12/14、步速 11/11、走法 17/17、风物 21/21、边界 15/15 个按钮至少一边不足 44 px。
- P0：广场卡片和详情把 `stops` 数组直接插值，页面出现 `[object Object]... 站`。
- P0：广场详情正文和部分长卡片被底部一级导航覆盖；长页面缺少统一的 `padding-bottom` 安全区。
- P1：六问内容集中在上半屏，下半屏大量留白；同时使用 8–11 px 小字承载状态、说明和可操作提示，阅读成本高。
- P1：路线、随逛、个人、广场四个模块的头部、色调、圆角、选中态和底栏视觉规则不统一，像四个独立 Demo 拼接。
- P1：随逛地图同屏点位与文字标签过多，地图成为“标签墙”；缺少清晰的搜索、图例、当前位置和结果数量层级。

## 逐页问题与优化方向

| 页面 | 主要问题 | 优化方向 | 优先级 |
| --- | --- | --- | --- |
| 01 启境首页 | 首屏同时出现主视觉、签语、输入卡、六问入口和双重导航，CTA 竞争；正文偏小 | 保留一个主 CTA；签语降为可折叠氛围信息；减少首屏卡片数量；正文不低于 14 px | P1 |
| 02 阿境开场 | 人物舞台占比过高，回答区几乎为空；“开始聊六问”视觉权重不足 | 舞台压至约 48–52%；让快捷回复靠近字幕；主 CTA 使用全宽高对比按钮 | P1 |
| 03 收帖页 | 仪式感完整，但信息和交互高度依赖低对比小字；返回/跳过触控区偏小 | 放大标题/说明对比度；顶部操作至少 44 px；减少底部辅助文案 | P2 |
| 04 心愿 | 6 个选项在狭小区域中承载图标、标题、副文案和勾选态，点击区不足；第三项替换规则解释太长 | 改为两列 56–64 px 高卡片；选择上限提示放入标题；替换时给轻量 Toast | P0 |
| 05 时间 | 时长、抵达、离开、路线说明同时呈现，层级复杂；地点卡片偏小 | 分两步渐进呈现：先时长、再地点；地点用 56 px 行高；路线说明移到确认后 | P0 |
| 06 步速 | 三档卡片视觉差异小，含义依赖细小副文案；大面积空白 | 使用场景化插图/时间示例；三档等高 64 px；选择后在下方即时解释安排影响 | P0 |
| 07 走法 | 同屏控件最多、布局最碎；交通、换酒店、转场冲突提示争夺注意力 | 拆为“交通偏好”和“住宿/转场”两段；用标准 Switch/Segmented Control；冲突说明贴近触发项 | P0 |
| 08 风物 | 已选项与候选标签重复出现，标签过密且触控不足 | 合并成单一可排序标签区；候选项最小高度 44 px；达到 5 项后禁用未选项并解释原因 | P0 |
| 09 边界 | 朱砂色使用面积突然增大，像错误态；完成卡、输入框、确认按钮三处争抢主操作 | 锁定态回归黔山绿，朱砂只做风险/解锁；“确认锁定”成为唯一主 CTA；完成卡合并进底部操作区 | P0 |
| 10 客态结晶 | 卡片信息偏细碎、字号过小；心愿为空时会留下弱内容区 | 建立“心愿/边界 > 时间/走法 > 标签”的顺序；关键值 16 px；空心愿显示明确补全入口 | P1 |
| 11 万象推演 | 氛围优秀，但步骤文字与底部提示对比不足；百分比与步骤可能传递重复进度 | 百分比保留，步骤只突出当前与已完成；正文提高对比；失败/本地接棒用独立状态卡 | P2 |
| 12 行程手帖 | 头部、回信、视图切换占用过多首屏；时间轴字号小；底栏遮挡长内容风险 | 结果头部折叠；回信收进信息卡；时间/标题/时长分别不低于 13/16/12 px；统一 96 px 底部安全区 | P1 |
| 13 路线显影 | 视觉地图不是实际可读地图，地点文字与路线关系弱；地图区偏小 | 地图扩至首屏核心区域；使用编号地点列表与地图联动；增加降级态、定位态和图例 | P1 |
| 14 微调 | Bottom sheet 内有 14 个按钮但 12 个不足触控标准；表单密度高、变化摘要位置靠后 | 所有 Segment 44 px 高；参数分组；变化摘要随修改即时更新；主操作固定但不遮挡输入 | P0 |
| 路线（实际页） | 沉浸感强，但人物占据约半屏，输入与主导航之间空间紧；页面无明显进度入口 | 缩短开场舞台；保持输入框在键盘上方；增加 1/6 进度和随时退出/暂存反馈 | P1 |
| 随逛 | 点位与标签密度过高，缺少主标题、搜索、结果数量和类别图例；地图控件贴近安全区 | 默认只显示高优先级点位，缩放后聚合；增加搜索/筛选抽屉、图例、当前位置；标签只显示选中点 | P0 |
| 个人 | 主页面品质较好，但首卡较高，下一模块只露出半屏；“查看行径/生成贵客帖”同权重 | 压缩行径卡约 15%；生成贵客帖设为主操作；照片横滑区提供完整卡片与滑动提示 | P1 |
| 个人·行径详情 | 弹窗可读，但大地图 + 5 行路线 + 双按钮使纵向拥挤；底部主导航仍可见造成层级冲突 | 模态打开时隐藏一级导航；路线区支持滚动；主按钮只保留“编辑贵客帖”，保存放入更多菜单 | P1 |
| 个人·贵客帖编辑 | 画布、贴纸工具、分享动作同屏拥挤；“发布广场”与本地保存同层且风险不同 | 工具改为选中贴纸后的浮动工具栏；保存/分享与发布分组；发布前增加预览确认 | P1 |
| 广场 | 首图过高，首屏看不到完整标题与作者；筛选项横向溢出且缺少滚动提示；浅色底栏与深色页面割裂 | Hero 高度降至 46–52vh；筛选做横滑并露出半个下一项；底栏加暗色适配或统一半透明材质 | P0 |
| 广场·行径详情 | 出现 `[object Object]`；主图占比过高；标题和数据行被压缩；底部导航覆盖详情 | `item.stops` 改用 `.length`；主图控制在 38–42vh；正文增加底部安全区；返回和收藏保持可见 | P0 |

## 修复实施计划

### 批次 A：可用性止血（P0）

目标：先让页面可读、可点、无遮挡、无明显数据错误。

1. 在 `app/merged.css` 建立移动端基础变量：正文 14 px、辅助文字 12 px、触控高度 44 px、页面底部安全区 `calc(96px + env(safe-area-inset-bottom))`。
2. 重构六问页共用壳层：统一 `convo-stage`、`convo-memory`、`convo-answer-panel`、`convo-composer` 的网格高度，让回答区占据主要可用空间并允许内部滚动。
3. 将心愿、时间、步速、走法、风物、边界、微调的按钮和分段控件全部提升到 44 px 以上。
4. 修复 `public/legacy/tab34/plaza-tab/plaza-tab.js` 中 `item.stops` 的展示，卡片、详情和地图摘要统一使用站点数量。
5. 为广场、个人弹层、结果页统一增加底部导航安全区；模态/全屏详情打开时隐藏一级导航。
6. 随逛默认隐藏非选中点文字标签，增加点位聚合与图例。

验收：360、390、430 px 三档无横向溢出；所有主要触控目标 ≥ 44 × 44 px；正文无遮挡；页面不再出现 `[object Object]`。

### 批次 B：信息架构与视觉层级（P1）

目标：减少“精致但拥挤”，让每屏只有一个明确主任务。

1. 首页只保留一个主入口，收拢签语和六问说明。
2. 六问改为“问题 → 选项 → 影响说明”的稳定模板，统一选中态和锁定态语义。
3. 结果页头部支持折叠，时间轴和地图成为页面主体。
4. 广场首图降高、筛选横滑、卡片标题提前进入首屏；个人首页压缩主卡并露出完整下一模块。
5. 统一四个一级模块的字体比例、圆角、底栏材质、激活色和图标尺寸。

验收：每屏只有一个主 CTA；首屏能完整看到主任务与至少一个关键结果；辅助文案不低于 12 px。

### 批次 C：状态与交互完善（P1/P2）

目标：补齐真实产品所需的系统状态。

1. 补齐 AI 生成中、AI 降级、无网络、定位拒绝、地图加载失败、空收藏、无附近结果。
2. 收帖和生成动效适配 `prefers-reduced-motion`。
3. 键盘弹出时缩小人物舞台，确保当前问题、输入框和发送按钮仍在同一视口。
4. 发布广场、系统分享、保存图片等动作增加明确结果反馈和必要确认。

### 批次 D：回归验证

1. 为 14 个启境屏幕和 4 个主导航页建立视觉快照。
2. 自动检查按钮最小尺寸、横向溢出、固定底栏遮挡、关键字号。
3. 手动回归六问完整链路、地图点位详情、个人行径弹层、贵客帖编辑、广场行径详情。
4. 在 360 × 800、390 × 844、430 × 932，以及带安全区的 iOS 视口完成验收。

## 建议的代码落点

- 启境共用布局与一级导航：`app/merged.css`、`app/page.tsx`
- 启境各屏结构：`app/spec/page.tsx`
- 随逛地图与详情：`public/legacy/tab2/stroll.css`、`public/legacy/tab2/stroll.js`
- 个人页与弹层：`public/legacy/tab34/personal-tab/personal-tab.css`、`public/legacy/tab34/personal-tab/personal-tab.js`
- 广场列表与详情：`public/legacy/tab34/plaza-tab/plaza-tab.css`、`public/legacy/tab34/plaza-tab/plaza-tab.js`

推荐按 A → B → C → D 顺序实施，不建议同时大改视觉主题；现有品牌资产值得保留，当前最有价值的提升来自可用性和层级重构。
